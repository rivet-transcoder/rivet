[package]
name = "rivet-frame"
version.workspace = true
edition.workspace = true
license-file = "LICENSE.md"
readme = "README.md"
repository.workspace = true
authors.workspace = true
description = "The value types shared by rivet's codec and container layers (StreamInfo, VideoFrame, colour metadata, EncodedPacket). Dependency-free, builds for every target including wasm32. Imported as `frame`."

[lib]
name = "frame"

[dependencies]
bytes.workspace = true
